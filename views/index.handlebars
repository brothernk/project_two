{{!-- Logo --}}
<div class="col-12">
  <div id="logo-main"></div>
</div>

{{!-- Content --}}
<div class="container">
  <div class="row">
    <div class="col-4" id="flame">
      <h3>Jokes</h3>
    </div>
    <div class="col-7" id="instruction-div">
      <h5 id="instructions">
        If you just want to judge randos on the internet go ahead - rate some sick burnz. Click the flame to see the jokes and then you literally just pick spicy or icy. That's it. Go forth and judge. Or, if you actually think you're funny go ahead an prove it...
      </h5>
      <div class="nav-bar">
        <a href="/edit" class="links" id="add-button">Add Your Own</a>


        <!-- Button trigger modal -->
        <a type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModalCenter" id="myModal">
          Launch demo modal
        </a>
        
        <!-- Modal -->
        <div class="modal fade" id="exampleModalCenter" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
          <div class="modal-dialog modal-dialog-centered" role="document">
            <div class="modal-content">
              <div class="modal-header">
                <h4 class="modal-title" id="exampleModalLongTitle" style="font-weight:bold">MAKE US LAUGH</h4>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                  <span aria-hidden="true">&times;</span>
                </button>
              </div>
              <div class="modal-body">
                <form id="joke-form">
                  <div class="form-group">
                    <label for="body">Joke:</label>
                    <textarea class="form-control" rows="3" id="body"></textarea>
                    <div class="form-group">
                      <label for="category">Select Category:</label>
                      <select class="form-control" id="category">
                      </select>
                    </div>
                    <br />
                    <button type="submit" class="btn btn-secondary submit" id="submit-button">Submit</button>
                  </div>
                </form>
                <div class="col-6" id="not-funny">
                  <h5>Realized you aren't actually funny? That's chill, go back to anonymously judging other people.</h5>
                  <div class="nav-bar">
                    <a href="/index" class="links" id="judge-button">Judge Jokes</a>
                  </div>
                </div>
              </div>
              <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                <button type="button" class="btn btn-primary">Save changes</button>
              </div>
            </div>
          </div>
        </div>
        <br>
        <a href="https://www.facebook.com/sharer/sharer.php?u=spicyburnz.com" target="_blank">
          Share on Facebook
        </a>
      </div>
    </div>
  </div>
</div>

<div class="container" id="joke-list">

  {{#each Post}}

    <div class="row" data-row-id="{{this.id}}" data-guessed="false" id="individual-joke">

      <div class="col-2 spicy-vote" data-id="{{this.id}}" data-spicy="{{this.spicy}}">
        <button class="btn btn-danger" id="spicy-button"><i class="fas fa-fire"></i></i> Spicy <div class="value">{{!-- {{this.spicy}} --}}</div></button>
      </div>

      <div class="col-8 mb-3">
        <p>{{this.entry}}</p> 
      </div>

      <div class="col-2 icy-vote" data-id="{{this.id}}" data-icy="{{this.icy}}">
        <button class="btn btn-info"><i class="far fa-snowflake"></i> Icy <div class="value">{{!-- {{this.icy}} --}}</div></button>
      </div>

    </div>

  {{/each}}

</div>